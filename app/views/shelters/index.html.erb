<h2>Shelters</h2>

<% @shelters.each do |shelter| %>
  <div id="shelter_<%= shelter.id %>"> <%= link_to shelter[:name], "/shelters/#{shelter.id}" %>
  <ul>
    <li><%= link_to "edit shelter", "/shelters/#{shelter.id}/edit" %></li>
    <li><%= link_to "delete shelter", "/shelters/#{shelter.id}", method: :delete %></li>
  </ul></div><br>
<% end %><br>


<%= link_to "New Shelter", '/shelters/new' %>
